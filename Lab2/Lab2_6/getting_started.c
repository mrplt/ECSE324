#include "address_map_arm.h"

extern int MAX_2(int x, int y);

int main() {
		int a[5] = {1,20,3,4,5};
		int length = 5;
		int max_val = a[0];
		c = MAX_2(a,b);
		int i = 1;
		for (i; i<n; i++){
		max_val = MAX_2(max_val, a[i]);
	}
		return max_val;
}